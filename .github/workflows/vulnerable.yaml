name: vulnerable

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  govulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: spotdemo4/nix/.github/actions/init@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - run: govulncheck ./...
